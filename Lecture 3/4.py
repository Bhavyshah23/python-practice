# Strore Above values in a list and & short them from "A" to "D".

students_grades = ["A", "B", "A", "C", "A", "B", "D"]
students_grades.sort()
print(students_grades)  # Sorting the list in ascending order
